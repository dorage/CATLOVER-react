import React from 'react';
import { Link, withRouter } from 'react-router-dom';
import styled from 'styled-components';

const InfiniteScrollPresenter = () => <></>;

export default withRouter(InfiniteScrollPresenter);
